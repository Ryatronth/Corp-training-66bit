server:
  port: 8090
  api_v1: "http://localhost:${server.port}/api/v1"

spring:
  web:
    resources:
      static-locations: ${STORAGE_DIR}
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  security:
    apiKey: ${API_KEY}